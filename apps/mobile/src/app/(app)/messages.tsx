import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function MessagesScreen() {
  return (
    <ScreenPlaceholder
      title="Messages"
      subtitle="Conversations and date planning will live here."
    />
  );
}

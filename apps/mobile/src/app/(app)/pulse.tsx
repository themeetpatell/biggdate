import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function PulseScreen() {
  return (
    <ScreenPlaceholder
      title="Pulse"
      subtitle="The BiggDate community feed will appear here."
    />
  );
}

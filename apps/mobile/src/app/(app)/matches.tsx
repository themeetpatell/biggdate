import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function MatchesScreen() {
  return (
    <ScreenPlaceholder
      title="Matches"
      subtitle="Curated introductions and intros will show up here."
    />
  );
}

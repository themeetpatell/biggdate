// Twitter card image — identical to the Open Graph image. Next picks this
// up for twitter:image; re-exporting keeps a single source of truth.
export { default, alt, size, contentType } from "./opengraph-image";
